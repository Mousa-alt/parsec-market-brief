# Runtime
httpx>=0.27          # every HTTP fetch: feeds, Yahoo quotes, SEC EDGAR
PyYAML>=6.0          # config.yaml
tzdata>=2024.1       # IANA timezone database — zoneinfo has no bundled copy on
                     # Windows or on slim Linux images, and every date in this
                     # app is market-local, not server-local.

# Tests
pytest>=8.0
pytest-asyncio>=0.23 # the compose path is async
